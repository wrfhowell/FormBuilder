import { If_Cond } from "../Nodes/If_Cond";

// const evaluateIfStatement = (node: If_Cond): string => {
//     return `if(${node.getCondition().getExpression()}) {
//         ${node.getCondBody().getExpression()}
//     }`;
// }
